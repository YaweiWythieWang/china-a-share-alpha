# Data

Raw and intermediate market data are intentionally excluded from the public repository.

The project uses Shanghai and Shenzhen A-share historical data obtained through Tushare-compatible APIs. Users should obtain their own authorized access and configure credentials locally.

See `REPRODUCIBILITY.md` for the expected local directory structure and pipeline.
